/**
 * Exercise 5
 * Spread in Functions: Create a function that accepts three arguments.
 * Then, create an array with three elements and call the function using the
 * spread operator with that array.
 */
export function spreadFunc(arg1, arg2, arg3) {
  console.log(arg1, arg2, arg3);
}

/**
 * Exercise 6
 * Merging Objects with Spread: Creates two objects with different properties.
 * Use the spread operator to merge these two objects into a new one.
 */
export const objectS1 = { name: "Laura" };
export const objectS2 = { course: "Spanish" };
